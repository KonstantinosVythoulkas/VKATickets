<!DOCTYPE html>
<html>
  <head>
    <title>Tickets</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css">    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>  
  <body>
  <% unless session[:user_id] == nil || current_page?(users_login_path) || current_page?(new_user_path)  || current_page?(users_login_shield_url) || current_page?(users_path)%>
    <%= render "layouts/navbar" %>
<% end %>
    <%= yield %>

        <%= render "layouts/footer" %>

  </body>
</html>
